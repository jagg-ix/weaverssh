# Optional MCP/control-plane profile for local_context_mcp_service.py and MCP helpers.
# Pin or constrain these with tools/packaging/lock_python_requirements.sh before release.
mcp>=1.9
uvicorn>=0.30
anyio>=4.0
